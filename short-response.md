# Answered Short response questions

## 1. Short Response
 One of them would have to make a repository and send an invite to the other person so they can both open their codespaces and work together

## 2. Short Response 
Developers use branches because it both have easy access so anyone can use them

## 3. Short Response
Git is a ditributed Version Control system, every developer has access to all changes and it gives you command line tool that allows you to track and manage changes to your code.
Git hub is a web based platform to store repos in the cloud and collaborate with other developers and it is user-friendly!
they both work together by both being meant to coding.

## 4. Short Response 
They put "git commit -m" first instead of "git add ." they were supposed to  write it like this:
git add .
git commit -m "saving work"
git push 
